#name: Metrics
#on:
 # schedule:
  #  - cron: "0 0 * * *"
  #workflow_dispatch:

#jobs:
 # github-metrics:
  #  runs-on: ubuntu-latest
   # timeout-minutes: 5
    #permissions:
     # contents: write
    #steps:
     # - uses: actions/checkout@v4

      #- uses: actions/setup-node@v4
       # with:
        #  node-version: "20"

     # - name: Fetch PageSpeed data
      #  continue-on-error: true
       # run: |
        #  curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://alextula.com&strategy=mobile&key=${{ secrets.PAGESPEED_TOKEN }}" \
         #   -o mobile.json || echo "Mobile API call failed"
          #curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://alextula.com&strategy=desktop&key=${{ secrets.PAGESPEED_TOKEN }}" \
          #  -o desktop.json || echo "Desktop API call failed"

     # - name: Render metrics SVG
      #  run: node render-metrics.js

     # - name: Commit
      #  run: |
       #   git config user.name "github-actions[bot]"
        #  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        #  git add github-metrics.svg
         # git diff --cached --quiet || (git commit -m "chore: update metrics" && git push)
